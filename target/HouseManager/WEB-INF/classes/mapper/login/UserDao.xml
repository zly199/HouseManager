<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.login.dao.UserDao">
    <select id="getMatchCount" resultType="Integer">
        SELECT count(*)
        FROM t_user tuser
        WHERE user_id =#{userId} and password=#{password}
    </select>

    <select id="findUserByUserId" resultType="cn.login.entity.User">
        SELECT user_id,user_name,sex,last_ip,password
        FROM t_user
        WHERE user_id = #{userId}
    </select>
    <update id="updateUser">
        update t_user set
        user_name = #{userName},
        password=#{password},
            sex=#{sex},
            last_ip=#{lastIp}
        where user_id = #{userId}
    </update>

</mapper>